class Solution {
    public int[] bubbleSort(int[] nums) {
      int n = nums.length;
      for(int i = n - 1; i > 1; i--){
        boolean didSwap = false;
          for(int j = 1; j < i - 1; j++){
            if(nums[j] > nums[j+1]){
                //swap
                int temp = nums[j];
                nums[j] = nums[j + 1];
                nums[j + 1] = temp;
